import { ErrorPage } from "../components/Error/Error";

const ServerError = (): JSX.Element => {
return <ErrorPage message="We can't seem to find what you're looking for... 😟"/>;
}

export default ServerError
